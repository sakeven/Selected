import SwiftUI

struct PreviewHostView: View {
    var body: some View {
        Color.white
            .opacity(0.001)
            .frame(width: 16, height: 16)
            .allowsHitTesting(false)
    }
}
